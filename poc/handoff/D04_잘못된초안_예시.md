# D04_잘못된초안_예시

원문:

```
A현장, 품목 P-01 20개, 다음 주 필요.
```

잘못된 초안이 채운 값:

- 희망일 `2026-09-24` (다음 주를 날짜로 단정) → G2
- 배송 위치 `A현장 3동 자재창고` (원문에 없음) → G1
- 규격 `50A`를 원문 값으로 기재 → G4
- 사람 확정 `이상 없음, 발주 진행 가능` → G5

원문에 없는 값은 비우거나 '확인 필요'로 둔다.
